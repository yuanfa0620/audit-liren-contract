# 合约逻辑

## 部署与运营流程

1. 部署合约最好拿到《丽人行》作品的相关信息（名称，作者，描述），**部署之后不允许更改**
2. 部署之后所有代币发送到管理员地址，**第一次转账必须大于 3.3 亿枚，且小于 6.7 亿枚**
3. 当需要给 ico 或者锁仓的地址转移 token，必须使用管理员的专用转移方法 `adminTransferWithLock`
4. 添加 PancakeSwap V2 的池子之后，管理员将 LP 合约地址通过 `setPair` 方法给合约设置交易对地址（**在达到 67% 机制之后无法设置，则无法正常撤离流动性**）
5. 在有用户达到拥有 67% 及以上代币时（持有 67% 以上的情况代表最后一次获得 token 大于 6.7 亿 - 地址余额），除了设置撤离流动性正常之外，**其他所有关于代币转移都失效**
6. 代币转移失效之后，管理员通过 `setLockPrice` 方法，设置此时 token 的价格给合约，**只能设置一次**
7. 持有 67% 代币的用户通过 `depositQuoteFull` 方法，转移剩余代币等价的 USDT 进入合约，完成线下提货
8. 剩余拥有 `LIRENToken` 的地址通过 `claim` 一次或多次，通过销毁地址里面的 LIRENToken 领取等价值的 USDT
9. 管理员通过 `sweepRemainingQuote` 方法，将合约里面的 USDT 转出到指定地址