# 合约逻辑

## 部署与运营流程

1. 部署合约最好拿到《丽人行》作品的相关信息（名称，作者，描述），**部署之后不允许更改**
2. 部署之后所有代币发送到管理员地址，**第一次转账必须大于 3.3 亿枚，且小于 6.7 亿枚**
3. 当需要给 ico 或者锁仓的地址转移 token，必须使用管理员的专用转移方法 `adminTransferWithLock`
4. 添加 PancakeSwap V2 的池子之后，管理员将 LP 合约地址通过 `setPair` 方法给合约设置交易对地址（**在达到 67% 机制之后无法设置，则无法正常撤离流动性**）
5. 冻结前交易会自动写入日价格；无成交日也可调用 `recordDailyPrice` 补记。可用 `previewLockPrice` 预览冻结时将采用的补偿单价（近 30 日均价，若无记录则用已注册 Pair 的 spot）
6. 在有用户达到拥有 67% 及以上代币时（持有 67% 以上的情况代表最后一次获得 token 大于 6.7 亿 - 地址余额），除了设置撤离流动性正常之外，**其他所有关于代币转移都失效**。合约自动开 Incident，并用日均价/spot **自动设价**（不可人工修改）
7. 持有 67% 代币的用户通过 `depositQuoteFull` 方法，转移剩余代币等价的 USDT 进入合约，完成线下提货
8. 剩余拥有 `LIRENToken` 的地址通过 `claim` 一次或多次，通过销毁地址里面的 LIRENToken 领取等价值的 USDT
9. Owner 通过 `setQuoteSweeper` 指定扫尾地址；巨鲸满额注入后满 `QUOTE_SWEEP_DELAY`（365 天），由 `quoteSweeper` 调用 `sweepRemainingQuote`，将合约内剩余 earmarked USDT 转出到指定地址
